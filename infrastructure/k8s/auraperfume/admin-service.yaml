---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: auraperfume
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: mohammedmusa1/perfume-admin-service:latest
          ports:
            - containerPort: 3008
          envFrom:
            - configMapRef:
                name: auraperfume-config
            - secretRef:
                name: auraperfume-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: auraperfume
spec:
  selector:
    app: admin-service
  ports:
    - port: 3008
      targetPort: 3008
  type: ClusterIP
