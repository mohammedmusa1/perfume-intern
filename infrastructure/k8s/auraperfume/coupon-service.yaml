---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coupon-service
  namespace: auraperfume
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coupon-service
  template:
    metadata:
      labels:
        app: coupon-service
    spec:
      containers:
        - name: coupon-service
          image: mohammedmusa1/perfume-coupon-service:latest
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef:
                name: auraperfume-config
            - secretRef:
                name: auraperfume-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: coupon-service
  namespace: auraperfume
spec:
  selector:
    app: coupon-service
  ports:
    - port: 3006
      targetPort: 3006
  type: ClusterIP
